# 🏛️ 《鬼灭之刃全明星》动态计数器架构与设计文档

本文档详细记录了 **Demon Slayer Dynamic Profile Counter** 的美术设计规范、图像处理算法以及 Serverless 架构设计原理。

---

## 🎨 一、美术设计考据规范

摒弃了传统 8bit 像素风或同质化呆板站姿，本套计数器采用了**二次元赛璐璐/厚涂 SD Q版风格**：

1. **画布与比例**：统一采用 2.5 头身 Q 版画风，保留动漫角色的发丝高光、服饰细节与瞳孔明暗。
2. **性格姿态解封**：
   - 0 炭治郎：温暖可靠的大哥哥姿态。
   - 1 祢豆子：萌系鸭子蹲姿。
   - 2 善逸：颜艺担当，内八字打颤流泪。
   - 3 伊之助：豪横野性咆哮，踩石举牌。
   - 4 香奈乎：抛金币优雅侧身。
   - 5 义勇：死鱼眼冷酷藏袖夹牌。
   - 6 胡蝶忍：踮脚舒展蝶翼。
   - 7 炼狱杏寿郎：豪爽大笑向前拍牌。
   - 8 甘露寺蜜璃：S型扭腰前倾爱心眼。
   - 9 无一郎：看云发呆挂牌坐姿。

---

## ✂️ 二、图像处理算法 (Flood-Fill Border Extraction)

### 1. 传统阈值抠图的缺陷
在处理手持白色卡牌的角色立绘时，若使用全局 RGB 色差阈值（`diff < threshold`），角色手持的**纯白底黑字卡牌**会被误识别为外围背景，导致扣成透明。在深色网页背景下透出黑底，产生“黑底黑字”的异常效果。

### 2. 外围连通域 FloodFill 算法
使用 Seed-based FloodFill 从图像四周 4 条边缘点向内部连通发散：
```
+-----------------------------------+
|  *   *   *   *   *   *   *   *   *|  <-- 边缘种子点 (Seeds)
| * +-----------------------------+ *|
| * |      [外围背景 抠除]         | *|
| * |        +----------+         | *|
| * |        | 角色立绘 |         | *|
| * |        | (含白卡) |         | *|
| * |        +----------+         | *|
| * +-----------------------------+ *|
+-----------------------------------+
```
- **外围背景**：与边界点连通，标注为背景并进行 Alpha 抗锯齿边缘羽化。
- **手持白卡**：位于角色身体内部，不与边界连通，被 100% 完整保留为纯白底！

---

## ⚡ 三、Serverless 零依赖架构 (Zero-Dependency API)

为了适应 GitHub Profile README 的安全防范机制（GitHub Camo Proxy 会缓存静态图片并剥离 JS）：

1. **Base64 WebP 内嵌**：全套 0~9 高清透明 WebP 压缩图以 Base64 编码内嵌于 `api/counter.js` 内存中。
2. **动态 SVG 矢量拼接**：后端根据请求的数值（如 `12345`）动态生成一行 `<svg>` 结构，使用 `<image href="data:image/webp;base64,...">` 节点将数字卡片精准拼接。
3. **No-Cache 响应标头**：
   ```http
   Content-Type: image/svg+xml
   Cache-Control: max-age=0, no-cache, no-store, must-revalidate, proxy-revalidate
   ```
   保证每次访问 GitHub 个人主页时均能实时触发递增计数与呈现！
